# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# LaTeX engine WASM + TeX Live data (~480 MB) — fetched via `npm run fetch:tex-assets`, never committed.
public/assets/busytex/

# Local env: real values live in .env.development / .env.production (gitignored).
# Commit only the .env.example template.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
