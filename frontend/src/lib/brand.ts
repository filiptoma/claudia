// The user-facing product name. Kept in one place so a future rebrand is a single edit.
// ("Claudia" remains the internal/business/project name; AstroNote is what users see.)
export const APP_NAME = 'AstroNote'

// Public source repository. AstroNote ships the AGPL-3.0 LaTeX engine (texlyre-busytex), so AGPL §13
// requires offering source to network users — surfaced via a visible "Source code" link (see §3.14).
export const SOURCE_URL = 'https://github.com/filiptoma/claudia'
